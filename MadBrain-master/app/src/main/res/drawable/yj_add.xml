<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 设置原角度 -->
    <corners android:radius="10dip" />

    <!-- 设置实心颜色 -->
    <solid android:color="@color/white" />

    <!-- 设置边框线 -->
    <stroke
        android:width="1dip"
        android:color="#c9c8c8" />
    <!-- 偏距 -->
    <!--
         <padding
        android:bottom="1dip"
        android:left="1dip"
        android:right="1dip"
        android:top="1dip" />
    -->
    
    <padding
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp" />
</shape> 